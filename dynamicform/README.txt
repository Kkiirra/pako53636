Добрый день, поскольку из условия проекта было сказано, что данные нужно сохранять
"Сохранять данные в модели в поле типа JSONB PostgreSQL (все добавленые инпуты)", я добавил в модель и миграцию
блоки try/except.

Так как с версии 4.0 django.contrib.postgres.fields было удалено и заменено на django.db.models,
но если запустить более раннюю версию джанго всё также будет работать.

(Test project from company)
